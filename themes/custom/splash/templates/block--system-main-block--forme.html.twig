
{%
  set classes = [
    'block',
    'block-' ~ configuration.provider|clean_class,
    'block-' ~ plugin_id|clean_class,
  ]
%}

    <div class="activite-btn">
      
      <a class="uk-button renforcement-musculaire" href="/renforcement-musculaire" >Renforcement Musculaire</a>
      <a class="uk-button cardio-trainning" href="/cardio-trainning" >Cardio Trainning</a>
      <a class="uk-button equilibre-et-souplesse" href="/equilibre-souplesse" >Équilibre & Souplesse</a>
      <a class="uk-button danse" href="/danse" >Danse</a>
    </div>
    <br>


{% if content %}

  <div{{ attributes.addClass(classes) }}>
    {{ title_prefix }}
    {% if label %}
      <h2{{ title_attributes }}>{{ label }}</h2>
    {% endif %}
    {{ title_suffix }}

    {% block content %}
      {{ content }}
    {% endblock %}
  </div>
{% endif %}